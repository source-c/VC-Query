#!/usr/bin/make -f
# -*- makefile -*-
# debian/rules for rolo package
# GNU copyright 2007 by Rafael Laboissiere

include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/cdbs/1/class/autotools.mk

clean::
	rm -f config.guess config.sub
